using System;
using System.Collections.Generic;
using System.Text;

namespace PointsPerGame.Core
{
	internal class Class1
	{
		/*
		
		OK, need figure out exactly how this works by checking the old version.
		Trace the working version through from the UI, through the service, through the scraper, through the data store, and back again.
		
		Then write it down in a document so you don't forget again.
				

		 */
	}
}
